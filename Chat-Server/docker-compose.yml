version: "2"
services:
  chat-server:
    build: .
    command: python -u app.py
    ports:
      - "5000:5000"
    volumes: [.:/app]
    environment:
      - DB_IP_ADDR=172.18.0.2
    links:
      - db
  db:
    image: mongo:3-xenial
